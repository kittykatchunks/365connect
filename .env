# ========================================
# Autocab365 PWA - Environment Variables
# ========================================

# ==================== Server Configuration ====================
# Node.js server configuration (server.js)

# Environment mode: development | production
NODE_ENV=development

# HTTP/HTTPS ports
HTTP_PORT=80
HTTPS_PORT=443

# ==================== Phantom API Configuration ====================
# Phantom PBX API settings

# PhantomID - 3-4 digit numeric identifier
PHANTOM_ID=388

# Full base URL of Phantom API (without /api path)
PHANTOM_API_BASE_URL=https://server1-388.phantomapi.net

# Phantom API authentication credentials
PHANTOM_API_USERNAME=ghost2
PHANTOM_API_KEY=aibiuFiquaeCuuve9Vohcheedee6ka

# Phantom API ports
PHANTOM_API_PORT=443
PHANTOM_NOAUTH_PORT=19773

# ==================== Development Settings ====================
# Only needed in development mode

# CORS proxy URL for development (bypasses CORS restrictions)
DEV_CORS_PROXY_URL=https://connect365.servehttp.com

# Vite dev server uses this for React app
VITE_DEV_CORS_PROXY_URL=https://connect365.servehttp.com

# ==================== SSL Certificates ====================
# Paths to SSL certificates (optional - defaults to ./certs/)

SSL_KEY_PATH=./certs/privkey.pem
SSL_CERT_PATH=./certs/fullchain.pem

# ==================== Busylight Configuration ====================
# Busylight hardware integration settings

BUSYLIGHT_WS_URL=ws://localhost:19774/ws
BUSYLIGHT_HTTP_HOST=http://localhost:19774

# ==================== API Request Flow ====================
#
# PRODUCTION (NODE_ENV=production or missing):
# - Authenticated calls: {PHANTOM_API_BASE_URL}:{PHANTOM_API_PORT}/api/
#   - Uses Basic Auth: {PHANTOM_API_USERNAME}:{PHANTOM_API_KEY}
# - NoAuth calls: {PHANTOM_API_BASE_URL}:{PHANTOM_NOAUTH_PORT}/api/
#
# DEVELOPMENT (NODE_ENV=development):
# - All calls route through: {DEV_CORS_PROXY_URL}/api/phantom/
# - Server proxies to actual Phantom API with authentication
# - Bypasses CORS restrictions during local development
#
# ==================== React App Integration ====================
#
# The React app (src/) uses PhantomApiService which:
# 1. Reads VITE_DEV_CORS_PROXY_URL in development
# 2. Uses local /api/ in production
# 3. PhantomID is stored in localStorage
# 4. All authentication handled server-side
