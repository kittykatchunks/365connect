# Multi-Server Credential Routing - Visual Guide

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         React App (Browser)                         │
│                                                                     │
│  • User enters PhantomID in settings (e.g., 388)                   │
│  • Stored in localStorage: PhantomID = "388"                       │
│  • PhantomApiService includes phantomId in all API calls           │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTP Request
                                    │ GET /api/phantom/AgentfromPhone?phantomId=388
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   Node.js Proxy Server (server.js)                  │
│                                                                     │
│  1. Extract PhantomID from query string: "388"                     │
│                                                                     │
│  2. Credential Lookup:                                             │
│     ┌─────────────────────────────────────────────┐               │
│     │ Check: PHANTOM_API_USERNAME_388             │               │
│     │ Check: PHANTOM_API_KEY_388                  │               │
│     │                                             │               │
│     │ ✅ Found? → Use specific credentials        │               │
│     │ ❌ Not found? → Use defaults                │               │
│     └─────────────────────────────────────────────┘               │
│                                                                     │
│  3. Build Target URL:                                              │
│     https://server1-388.phantomapi.net/api/AgentfromPhone          │
│                                                                     │
│  4. Add Authentication Header:                                     │
│     Authorization: Basic base64(username:apiKey)                   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ Authenticated Request
                                    │ Authorization: Basic Z2hvc3QyOmFpYml1...
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│              Phantom PBX Server (server1-388.phantomapi.net)        │
│                                                                     │
│  • Validates credentials                                           │
│  • Processes API request                                           │
│  • Returns JSON response                                           │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ JSON Response
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   Node.js Proxy Server (server.js)                  │
│                                                                     │
│  • Receives response from Phantom server                           │
│  • Forwards response to React app                                  │
│  • Logs response details                                           │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ JSON Response
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         React App (Browser)                         │
│                                                                     │
│  • PhantomApiService receives response                             │
│  • Processes data                                                  │
│  • Updates UI                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

## Credential Lookup Flow

```
┌──────────────────────────┐
│ Request Arrives          │
│ phantomId = 388          │
└────────────┬─────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────┐
│ Step 1: Check for Server-Specific Credentials           │
│                                                          │
│ const specificUsernameKey = `PHANTOM_API_USERNAME_388`  │
│ const specificKeyKey = `PHANTOM_API_KEY_388`            │
│                                                          │
│ apiUsername = process.env[specificUsernameKey]          │
│ apiKey = process.env[specificKeyKey]                    │
└────────────┬─────────────────────────────────────────────┘
             │
             ▼
        ┌────────┐
        │ Found? │
        └───┬────┘
            │
     ┌──────┴──────┐
     │             │
    YES           NO
     │             │
     ▼             ▼
┌─────────────┐  ┌──────────────────────────────┐
│ Use         │  │ Step 2: Fallback to Defaults │
│ Specific    │  │                              │
│ Credentials │  │ apiUsername =                │
│             │  │   process.env.PHANTOM_API_   │
│ Username:   │  │   USERNAME                   │
│ ghost2      │  │                              │
│             │  │ apiKey =                     │
│ Key:        │  │   process.env.PHANTOM_API_   │
│ aibi***     │  │   KEY                        │
└─────────────┘  └──────────────────────────────┘
     │                      │
     └──────────┬───────────┘
                │
                ▼
┌────────────────────────────────┐
│ Set Authorization Header       │
│                                │
│ const authString =             │
│   Buffer.from(                 │
│     `${username}:${apiKey}`    │
│   ).toString('base64')         │
│                                │
│ Authorization:                 │
│   Basic Z2hvc3QyOmFpYml1...    │
└────────────────────────────────┘
                │
                ▼
┌────────────────────────────────┐
│ Forward to Phantom Server      │
│                                │
│ https://server1-388            │
│   .phantomapi.net/api/         │
│   AgentfromPhone               │
└────────────────────────────────┘
```

## Example: Three Different Servers

```
┌─────────────────────────────────────────────────────────────┐
│                      .env Configuration                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ # Default (fallback)                                        │
│ PHANTOM_API_USERNAME=defaultuser                            │
│ PHANTOM_API_KEY=defaultkey                                  │
│                                                             │
│ # Server 388 - Production                                   │
│ PHANTOM_API_USERNAME_388=ghost2                             │
│ PHANTOM_API_KEY_388=aibiuFiquaeCuuve9Vohcheedee6ka          │
│                                                             │
│ # Server 375 - Staging                                      │
│ PHANTOM_API_USERNAME_375=admin375                           │
│ PHANTOM_API_KEY_375=StagingKey375                           │
│                                                             │
│ # Server 420 - Development                                  │
│ PHANTOM_API_USERNAME_420=dev420                             │
│ PHANTOM_API_KEY_420=DevKey420                               │
└─────────────────────────────────────────────────────────────┘
                               │
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                    │
          ▼                    ▼                    ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│ Request:         │ │ Request:         │ │ Request:         │
│ phantomId=388    │ │ phantomId=375    │ │ phantomId=420    │
├──────────────────┤ ├──────────────────┤ ├──────────────────┤
│ Uses:            │ │ Uses:            │ │ Uses:            │
│ ghost2           │ │ admin375         │ │ dev420           │
│ aibi***          │ │ Stag***          │ │ DevK***          │
├──────────────────┤ ├──────────────────┤ ├──────────────────┤
│ Routes to:       │ │ Routes to:       │ │ Routes to:       │
│ server1-388      │ │ server1-375      │ │ server1-420      │
│ .phantomapi.net  │ │ .phantomapi.net  │ │ .phantomapi.net  │
└──────────────────┘ └──────────────────┘ └──────────────────┘
```

## Request Example with Logging

```
User makes API call from React app
↓
PhantomApiService.post('AgentfromPhone', { phone: '1001' })
↓
Sends HTTP Request:
GET /api/phantom/AgentfromPhone?phantomId=388&phone=1001

════════════════════════════════════════════════════════════════
[HTTPS REQUEST] 2026-01-21T10:30:45.123Z
════════════════════════════════════════════════════════════════
  Method:      POST
  Input URL:   https://connect365.example.com/api/phantom/AgentfromPhone?phantomId=388
  Path:        /api/phantom/AgentfromPhone?phantomId=388
  Type:        PHANTOM API PROXY
────────────────────────────────────────────────────────────────

  🔄 PROXY TRANSLATION:
     Input:  https://connect365.example.com/api/phantom/AgentfromPhone?phantomId=388
     Output: https://server1-388.phantomapi.net/api/AgentfromPhone
     PhantomID: 388
     Credentials Source: Specific (PHANTOM_API_USERNAME_388)  ← KEY INFO!
     Auth: ✅ Basic Auth (ghost2)
     Authorization Header: Basic Z2hvc3QyOmFpYml1Rml...
     Credentials: ghost2:aibi***
     Query Params:
       • phantomId = 388
       • phone = 1001

  ✅ PROXY RESPONSE:
     Status: 200 OK
     Content-Type: application/json
     Body Preview: {"success":true,"agent":"John Doe","extension":"1001"}

  ✅ Response:   200 OK
  Duration:    142ms
  Size:        156 bytes
════════════════════════════════════════════════════════════════
```

## Benefits of This Approach

```
┌──────────────────────────────────────────────────────────┐
│                     ✅ Advantages                        │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ 1. Centralized Management                                │
│    • All credentials in one .env file                    │
│    • Easy to update and rotate                           │
│    • No code changes needed                              │
│                                                          │
│ 2. Automatic Routing                                     │
│    • Proxy auto-selects correct credentials              │
│    • Based on phantomId from client                      │
│    • No manual configuration                             │
│                                                          │
│ 3. Backward Compatible                                   │
│    • Existing single-server setups work                  │
│    • Default credentials for fallback                    │
│    • No client-side changes required                     │
│                                                          │
│ 4. Secure                                                │
│    • Credentials never exposed to client                 │
│    • Server-side authentication only                     │
│    • Basic Auth over HTTPS                               │
│                                                          │
│ 5. Observable                                            │
│    • Verbose logging shows credential source             │
│    • Easy to debug routing issues                        │
│    • Track which servers are being used                  │
│                                                          │
│ 6. Scalable                                              │
│    • Add new servers by adding env vars                  │
│    • No code deployment required                         │
│    • Support unlimited servers                           │
└──────────────────────────────────────────────────────────┘
```

## Credential Priority Logic

```
┌─────────────────────────────────────────────────────────────┐
│                    Priority Resolution                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Priority 1: Server-Specific Credentials                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ PHANTOM_API_USERNAME_{phantomId}                    │   │
│  │ PHANTOM_API_KEY_{phantomId}                         │   │
│  │                                                     │   │
│  │ Example for server 388:                            │   │
│  │   PHANTOM_API_USERNAME_388=ghost2                  │   │
│  │   PHANTOM_API_KEY_388=aibi***                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                 │
│                           ▼                                 │
│                    ┌─────────────┐                         │
│                    │ Not Found?  │                         │
│                    └──────┬──────┘                         │
│                           │                                 │
│                           ▼                                 │
│  Priority 2: Default Credentials                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ PHANTOM_API_USERNAME                                │   │
│  │ PHANTOM_API_KEY                                     │   │
│  │                                                     │   │
│  │ Used as fallback for all servers without           │   │
│  │ specific credentials                                │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## Configuration Matrix

```
╔═══════════════╦═════════════════════════════╦═══════════════════╗
║ PhantomID     ║ Credentials Used            ║ Source            ║
╠═══════════════╬═════════════════════════════╬═══════════════════╣
║ 388           ║ ghost2:aibi***              ║ SPECIFIC          ║
║ 375           ║ admin375:Stag***            ║ SPECIFIC          ║
║ 420           ║ dev420:DevK***              ║ SPECIFIC          ║
║ 999 (new)     ║ defaultuser:defaultkey      ║ DEFAULT (fallback)║
╚═══════════════╩═════════════════════════════╩═══════════════════╝
```

## Security Model

```
┌──────────────────────────────────────────────────────────────┐
│                    🔒 Security Layers                        │
└──────────────────────────────────────────────────────────────┘

Layer 1: Client Side
┌────────────────────────────────────────────────────────┐
│ • No credentials stored                                │
│ • Only PhantomID in localStorage                       │
│ • PhantomID sent as query parameter                    │
│ • HTTPS encryption for all requests                    │
└────────────────────────────────────────────────────────┘

Layer 2: Proxy Server
┌────────────────────────────────────────────────────────┐
│ • Credentials in .env file (not in code)              │
│ • .env excluded from version control                   │
│ • Dynamic credential lookup                            │
│ • Basic Auth header added server-side                  │
└────────────────────────────────────────────────────────┘

Layer 3: Transport
┌────────────────────────────────────────────────────────┐
│ • HTTPS/TLS encryption                                 │
│ • Authorization header in transit                      │
│ • Secure WebSocket (WSS) for SIP                       │
└────────────────────────────────────────────────────────┘

Layer 4: Phantom Server
┌────────────────────────────────────────────────────────┐
│ • Validates Basic Auth credentials                     │
│ • Authenticates each request                           │
│ • Returns 401 if credentials invalid                   │
└────────────────────────────────────────────────────────┘
```

## Troubleshooting Decision Tree

```
                    ┌─────────────────────┐
                    │ API Call Failing?   │
                    └──────────┬──────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │ Check Proxy Logs    │
                    └──────────┬──────────┘
                               │
              ┌────────────────┴────────────────┐
              │                                 │
              ▼                                 ▼
   ┌──────────────────────┐        ┌──────────────────────┐
   │ "Missing             │        │ "401 Unauthorized"   │
   │  credentials"        │        │                      │
   └──────────┬───────────┘        └──────────┬───────────┘
              │                                │
              ▼                                ▼
   ┌──────────────────────┐        ┌──────────────────────┐
   │ Add credentials      │        │ Check credentials    │
   │ to .env:             │        │ are correct:         │
   │                      │        │                      │
   │ PHANTOM_API_         │        │ • Username matches   │
   │ USERNAME_388=...     │        │ • API key valid      │
   │                      │        │ • No typos           │
   │ PHANTOM_API_KEY_     │        │                      │
   │ 388=...              │        │ Verify in .env       │
   └──────────┬───────────┘        └──────────┬───────────┘
              │                                │
              │                                │
              └────────────┬───────────────────┘
                           │
                           ▼
                ┌──────────────────────┐
                │ Restart Server       │
                │                      │
                │ npm run dev          │
                │ or                   │
                │ node server.js       │
                └──────────┬───────────┘
                           │
                           ▼
                ┌──────────────────────┐
                │ Test Again           │
                └──────────────────────┘
```

## Summary Checklist

```
✅ Implementation Checklist

Server Configuration:
☑ server.js updated with dynamic credential lookup
☑ onProxyReq handler checks for server-specific credentials
☑ Falls back to default credentials if not found
☑ Logs credential source for debugging

Environment Setup:
☑ .env file structured for multi-server
☑ Default credentials defined
☑ Server 388 credentials added
☑ Template for additional servers included
☑ .env added to .gitignore

Documentation:
☑ MULTI_SERVER_CREDENTIALS.md - Complete guide
☑ MULTI_SERVER_QUICKSTART.md - Quick reference
☑ MULTI_SERVER_ARCHITECTURE_VISUAL.md - This file
☑ .env.example - Template for deployments

Client Compatibility:
☑ No changes required to React app
☑ PhantomApiService already sends phantomId
☑ Backward compatible with existing setup
☑ Works with existing API calls

Security:
☑ Credentials in .env (not in code)
☑ .env excluded from git
☑ Server-side authentication only
☑ HTTPS encryption enforced

Testing:
☑ Logs show credential source
☑ Easy to verify routing
☑ Clear error messages
☑ Detailed request/response logging
```

## Next Steps

1. **Add credentials for new servers** in `.env` file
2. **Restart proxy server** to load new credentials
3. **Test with different PhantomIDs** to verify routing
4. **Monitor logs** to confirm correct credential usage
5. **Deploy to production** with environment variables

Ready to go! 🚀
